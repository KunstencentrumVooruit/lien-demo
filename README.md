# Get democrazy projects from API and feed them to excel.
# This code can be reused if you have custom data combined with api data
# Custom data gets saved with ID and afterwards placed in the correct cells.
